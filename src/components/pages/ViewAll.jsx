import React from 'react'
import Bulma from 'bulma'
import { Box, Columns, Container, Column, Select, Content, Title, Button, Pagination, PageControl, PageList, Page, PageEllipsis, PageLink, PanelIcon, PanelBlock, PanelHeading, Panel, Control, Media, MediaContent, MediaLeft, Input, Image } from "bloomer"

const ViewAll = () => {
  return (
      <div>
          <h1>Removed for reworking</h1>
      </div>
  )
}

export default ViewAll
